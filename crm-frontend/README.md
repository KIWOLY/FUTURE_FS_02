# CRM Frontend

React + Tailwind admin dashboard for managing leads.

## Setup
```bash
npm install
cp .env.example .env
npm run dev
```

## Environment
- `VITE_API_BASE_URL` (example: `http://localhost:8000`)

## Notes
- Requires a valid JWT from the backend login endpoint.
- Access logs page at `/logs` (superuser only).
